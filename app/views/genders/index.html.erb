<h2>The Following Genders Are Currently Being Used</h2>
<% @genders.each do |gender| %>
<div>
  <% if gender == "unknown" %>
    Some genders have not been determined, update genders to assign rough (unverified) genders.
    <%= link_to "Update genders", new_gender_path %>
  <% else %>
    <strong><%= gender %> </strong>
  <% end %>
</div>
<% end %>
